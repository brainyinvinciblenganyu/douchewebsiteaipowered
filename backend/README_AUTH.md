# Auth backend (Neon Postgres + session cookies)

This directory will contain all API implementation logic.

## Required env vars
- `DATABASE_URL`
- `AUTH_SESSION_SECRET`

Copy:
- `backend/.env.example` -> `backend/.env.local` (or `backend/.env`)

